let BackEndUrl = `https://canyonroadfinder.herokuapp.com`;
export default BackEndUrl;

// This is the backend url, this is so if the url changes, it just needs to be changes in one place instead of looking through the entire application for it
